# ============================================================
# Generated by CI — do not edit on server, edit .env.tmpl
# ============================================================

# --- Images ---
BACKEND_IMAGE=${BACKEND_IMAGE}
IMAGE_REGISTRY=${IMAGE_REGISTRY}

# --- PostgreSQL ---
POSTGRES_DB=${POSTGRES_DB}
POSTGRES_USER=${POSTGRES_USER}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}

# --- Backend runtime ---
PORT=:8080
HOST_PORT=8080
DATABASE_URL=${DATABASE_URL}
JWT_SIGNING_KEY=${JWT_SIGNING_KEY}
ACCESS_TOKEN_TTL=${ACCESS_TOKEN_TTL}
REFRESH_TOKEN_TTL=${REFRESH_TOKEN_TTL}
EMAIL_CODE_TTL=${EMAIL_CODE_TTL}
SMS_CODE_TTL=${SMS_CODE_TTL}
SMS_RATE_WINDOW=${SMS_RATE_WINDOW}
SMS_RESEND_INTERVAL=${SMS_RESEND_INTERVAL}
SMS_MAX_PER_PHONE=${SMS_MAX_PER_PHONE}
SMS_MAX_PER_IP=${SMS_MAX_PER_IP}
PASSWORD_RESET_TTL=${PASSWORD_RESET_TTL}
SWAGGER_ENABLED=${SWAGGER_ENABLED}

# --- LiteLLM ---
LITELLM_BASE_URL=${LITELLM_BASE_URL}
LITELLM_DEFAULT_MODEL=${LITELLM_DEFAULT_MODEL:-gpt-4o-mini}
LITELLM_MASTER_KEY=${LITELLM_MASTER_KEY}
LITELLM_HTTP_TIMEOUT=${LITELLM_HTTP_TIMEOUT}
LITELLM_DEFAULT_USER_QUOTA=${LITELLM_DEFAULT_USER_QUOTA}

# --- AuthZ ---
AUTHZ_PROVIDER=${AUTHZ_PROVIDER}
OPENFGA_API_URL=${OPENFGA_API_URL}
OPENFGA_STORE_ID=${OPENFGA_STORE_ID}
OPENFGA_DATASTORE_URI=${DATABASE_URL}
HOST_OPENFGA_PORT=8081

# --- SMS (Aliyun) ---
SMS_PROVIDER=${SMS_PROVIDER}
ALIBABA_CLOUD_REGION_ID=${ALIBABA_CLOUD_REGION_ID}
ALIBABA_CLOUD_ACCESS_KEY_ID=${ALIBABA_CLOUD_ACCESS_KEY_ID}
ALIBABA_CLOUD_ACCESS_KEY_SECRET=${ALIBABA_CLOUD_ACCESS_KEY_SECRET}
ALIYUN_SMS_SIGN_NAME=${ALIYUN_SMS_SIGN_NAME}
ALIYUN_SMS_TEMPLATE_CODE_REGISTER=${ALIYUN_SMS_TEMPLATE_CODE_REGISTER}

# --- Frontend ---
VITE_API_BASE_URL=${VITE_API_BASE_URL}
