name: tencent-cvm-deploy

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag to deploy (e.g. sha-abc1234). Empty = current commit tag"
        required: false
        default: ""

permissions:
  contents: read

concurrency:
  group: tencent-cvm-deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    # Temporarily disabled: keep workflow definition for future re-enable.
    if: ${{ false }}
    runs-on: ubuntu-latest
    env:
      ACR_REGISTRY: ${{ secrets.ALIYUN_ACR_REGISTRY }}
      ACR_NAMESPACE: ${{ secrets.ALIYUN_ACR_NAMESPACE }}
      IMAGE_REPO: backend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Validate deploy secrets
        run: |
          set -euo pipefail
          test -n "${ACR_REGISTRY}" || (echo "ALIYUN_ACR_REGISTRY is required" && exit 1)
          test -n "${ACR_NAMESPACE}" || (echo "ALIYUN_ACR_NAMESPACE is required" && exit 1)
          test -n "${{ secrets.CVM_HOST }}" || (echo "CVM_HOST is required" && exit 1)
          test -n "${{ secrets.CVM_USER }}" || (echo "CVM_USER is required" && exit 1)
          test -n "${{ secrets.CVM_DEPLOY_PATH }}" || (echo "CVM_DEPLOY_PATH is required" && exit 1)

      - name: Compute image tag
        id: vars
        run: |
          SHORT_SHA="${GITHUB_SHA::7}"
          if [ -n "${{ github.event.inputs.image_tag }}" ]; then
            TAG="${{ github.event.inputs.image_tag }}"
          else
            TAG="sha-${SHORT_SHA}"
          fi
          IMAGE="${ACR_REGISTRY}/${ACR_NAMESPACE}/${IMAGE_REPO}:${TAG}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "image=${IMAGE}" >> "$GITHUB_OUTPUT"
          echo "Using image: ${IMAGE}"

      - name: Log in to Aliyun ACR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.ACR_REGISTRY }}
          username: ${{ secrets.ALIYUN_ACR_USERNAME }}
          password: ${{ secrets.ALIYUN_ACR_PASSWORD }}

      - name: Build and push image
        if: github.event_name == 'push' || github.event.inputs.image_tag == ''
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.backend
          push: true
          tags: |
            ${{ steps.vars.outputs.image }}
            ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/${{ env.IMAGE_REPO }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Prepare SSH key
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "${{ secrets.CVM_SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Copy deploy files to CVM
        run: |
          SSH_PORT="${{ secrets.CVM_PORT }}"
          if [ -z "$SSH_PORT" ]; then SSH_PORT=22; fi
          ssh -p "$SSH_PORT" -o StrictHostKeyChecking=no "${{ secrets.CVM_USER }}@${{ secrets.CVM_HOST }}" "mkdir -p '${{ secrets.CVM_DEPLOY_PATH }}'"
          scp -P "$SSH_PORT" -o StrictHostKeyChecking=no \
            deploy/ecs/docker-compose.prod.yml \
            deploy/ecs/.env.tmpl \
            "${{ secrets.CVM_USER }}@${{ secrets.CVM_HOST }}:${{ secrets.CVM_DEPLOY_PATH }}/"
          ssh -p "$SSH_PORT" -o StrictHostKeyChecking=no "${{ secrets.CVM_USER }}@${{ secrets.CVM_HOST }}" \
            "mv '${{ secrets.CVM_DEPLOY_PATH }}/docker-compose.prod.yml' '${{ secrets.CVM_DEPLOY_PATH }}/docker-compose.yml'"

      - name: Deploy on CVM
        env:
          BACKEND_IMAGE: ${{ steps.vars.outputs.image }}
          IMAGE_REGISTRY: ${{ secrets.IMAGE_REGISTRY }}
          POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
          POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          JWT_SIGNING_KEY: ${{ secrets.JWT_SIGNING_KEY }}
          LITELLM_BASE_URL: ${{ secrets.LITELLM_BASE_URL }}
          LITELLM_MASTER_KEY: ${{ secrets.LITELLM_MASTER_KEY }}
          OPENFGA_STORE_ID: ${{ secrets.OPENFGA_STORE_ID }}
          ALIBABA_CLOUD_REGION_ID: ${{ secrets.ALIBABA_CLOUD_REGION_ID }}
          ALIBABA_CLOUD_ACCESS_KEY_ID: ${{ secrets.ALIBABA_CLOUD_ACCESS_KEY_ID }}
          ALIBABA_CLOUD_ACCESS_KEY_SECRET: ${{ secrets.ALIBABA_CLOUD_ACCESS_KEY_SECRET }}
          ALIYUN_SMS_SIGN_NAME: ${{ secrets.ALIYUN_SMS_SIGN_NAME }}
          ALIYUN_SMS_TEMPLATE_CODE_REGISTER: ${{ secrets.ALIYUN_SMS_TEMPLATE_CODE_REGISTER }}
          VITE_API_BASE_URL: ${{ secrets.VITE_API_BASE_URL }}
        run: |
          SSH_PORT="${{ secrets.CVM_PORT }}"
          if [ -z "$SSH_PORT" ]; then SSH_PORT=22; fi

          # Render .env locally from template, then push to CVM
          envsubst < deploy/ecs/.env.tmpl > /tmp/.env.rendered

          scp -P "$SSH_PORT" -o StrictHostKeyChecking=no \
            /tmp/.env.rendered \
            "${{ secrets.CVM_USER }}@${{ secrets.CVM_HOST }}:${{ secrets.CVM_DEPLOY_PATH }}/.env"

          ssh -p "$SSH_PORT" -o StrictHostKeyChecking=no "${{ secrets.CVM_USER }}@${{ secrets.CVM_HOST }}" << 'EOF'
            set -euo pipefail
            cd "${{ secrets.CVM_DEPLOY_PATH }}"
            echo "${{ secrets.ALIYUN_ACR_PASSWORD }}" | docker login "${{ secrets.ALIYUN_ACR_REGISTRY }}" -u "${{ secrets.ALIYUN_ACR_USERNAME }}" --password-stdin
            docker compose pull
            docker compose up -d
          EOF

      - name: Health check
        run: |
          ADDR="${{ secrets.APP_HEALTHCHECK_ADDRESS }}"
          URL=""

          if [ -z "$ADDR" ]; then
            URL="http://${{ secrets.CVM_HOST }}:8080/healthz"
          elif [[ "$ADDR" == http://* || "$ADDR" == https://* ]]; then
            URL="$ADDR"
          else
            if [[ "$ADDR" =~ :443($|/) ]]; then
              SCHEME="https"
            elif [[ "$ADDR" =~ :80($|/) ]]; then
              SCHEME="http"
            elif [[ "$ADDR" == *:* ]]; then
              SCHEME="http"
            else
              SCHEME="https"
            fi
            URL="${SCHEME}://${ADDR}"
          fi

          if [[ "$URL" =~ ^https?://[^/]+$ ]]; then
            URL="${URL}/healthz"
          fi

          for i in $(seq 1 30); do
            if curl -fsS "$URL" >/dev/null; then
              echo "Health check passed: $URL"
              exit 0
            fi
            sleep 5
          done
          echo "Health check failed: $URL"
          exit 1
